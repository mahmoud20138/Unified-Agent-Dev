# OpenCode adapter

The installer merges the shared MCP server into the user's active `opencode.json` or `opencode.jsonc` and installs the generated `unified-agent-dev` skill. JSONC comments and trailing commas are preserved through structured edits.
